{
  "featureId": "m3-fix-base-url-switch-stale-credential-leak",
  "reviewedAt": "2026-03-13T15:19:43Z",
  "commitId": "fbf80d40009bda799ccf5e0977e124e377e1dcc5",
  "transcriptSkeletonReviewed": true,
  "diffReviewed": true,
  "status": "pass",
  "codeReview": {
    "summary": "Pass. The fix directly resolves the prior blocking stale-credential leak by caching the previous node under its normalized base URL, then restoring credentials only from the selected target's cache and otherwise clearing active `zelidauth` and `enterpriseKey`. The reviewed scope still preserves cached-target adoption, gateway pinning, and read-only/failed-path behavior from the original node-helper slice, and the added regressions cover both `use-gateway` and `use-base-url` uncached switches plus follow-up `auth status` checks.",
    "issues": []
  },
  "sharedStateObservations": [
    {
      "area": "knowledge",
      "observation": "`.factory/library/environment.md` still documents the stale-credential limitation as current behavior even though this fix removes it, so future workers could rely on outdated guidance about uncached base-url switches.",
      "evidence": "`.factory/library/environment.md:18` says uncached target switches still retain the previous node's credentials until a follow-up fix lands, but `fluxos-cli/src/state/sessionState.ts:317-333` now clears active auth/key when the selected normalized base URL has no cache entry and `fluxos-cli/test/node-helpers.test.ts:448-495,561-608` covers both uncached `use-gateway` and `use-base-url` paths."
    },
    {
      "area": "skills",
      "observation": "The `cli-command-worker` procedure does not tell workers to read `.factory/library/environment.md`, which leaves them prone to missing environment-specific constraints such as the lack of `jq` in this mission environment.",
      "evidence": "`.factory/skills/cli-command-worker/SKILL.md` only requires `architecture.md`, `shared-runtime.md`, and `user-testing.md`. The transcript skeleton for worker session `ee0a0b6b-d625-40c0-8ed7-2a030f8dc75d` shows an `Execute` step running `jq` against `features.json` before a later `Read` of `.factory/library/environment.md`, while `.factory/library/environment.md:20` explicitly says `jq` is not installed."
    }
  ],
  "addressesFailureFrom": "/home/stache/projects/flux-skills/.factory/validation/m3-auth-node/scrutiny/reviews/m3-node-resolution-and-base-url-helpers.json",
  "summary": "Pass. I reviewed the fix transcript skeleton plus both the original failing diff and the fix diff. The updated base-url switch helper now clears active auth and enterprise-key state for uncached targets while preserving per-base-url cache adoption for cached targets, which adequately closes the prior blocking issue without introducing new scoped code problems. I also noted two shared-state gaps: the environment library still describes the old limitation as active, and the CLI command worker skill omits `environment.md`, which contributed to an avoidable `jq` attempt in this environment."
}
