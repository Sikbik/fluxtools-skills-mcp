# `/payment/*` — Payment Helpers

The node exposes small helpers for generating and verifying payment requests.

See the full list in `references/endpoints-inventory.md` (category: `payment`).

- `GET /payment/paymentrequest`
- `POST /payment/verifypayment`
