# GitHub Actions Examples

This folder contains a minimal workflow that:

- installs Node dependencies
- builds `fluxos-cli`
- verifies an app spec
- prepares a plan artifact
- shows how a later step could submit with a signature

See [`register-from-plan.yml`](./register-from-plan.yml).
